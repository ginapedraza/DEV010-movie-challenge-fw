/*import Home from './Home'; // Asegúrate de que la ruta sea correcta
import '@testing-library/jest-dom/extend-expect';
import { render, screen } from '@testing-library/react';


describe('Home', () => {
  test('it should render the Home component', () => {
    render(<Home />);
    const posterTitle = screen.getByText('Uncover Reality, One Film at a Time...'); // Debe coincidir con el contenido del botón en Header
    expect(posterTitle).toBe('Uncover Reality, One Film at a Time...');

  });

  // Agrega más pruebas aquí según sea necesario
});*/






